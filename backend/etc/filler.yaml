log:
  # please run it
  # with "info" mode in
  # production!
  level: "debug"

database:
  user: postgres
  password: postgres
  database: app

filler:
  concurrency: 50
  reload_period: 1

geth:
  # local node (fast)
  endpoint: "http://127.0.0.1:8545"
  # or remove one (slow)
  # endpoint: "https://sidechain.livenet.sonm.com"
