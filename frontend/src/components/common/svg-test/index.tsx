import * as React from 'react';

//const AddIcon = require('./add.svg').default;
const Icon = require('./add.svg').default;
export class SvgTest extends React.Component {
    public render() {
        return (
            <div>
                <Icon/>
            </div>
        );
    }
}
